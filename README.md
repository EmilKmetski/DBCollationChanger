# DBCollationChanger
Application generates the DB changes collation for you so you can have automatic colation change whithout all the manual stuff. 
Actually it first generates changes script from the sql server and save it in Temp dir in windows and after that executes the script to change the collation. 
The script is very complicated it is inside the code. 
